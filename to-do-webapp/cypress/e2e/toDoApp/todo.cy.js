/// <reference types="cypress" />

describe('to-do app', () => {
  // beforeEach(() => {
  //   cy.visit('https://example.cypress.io/todo')
  // })

  it('check the title of the app', () => {

  });
});
